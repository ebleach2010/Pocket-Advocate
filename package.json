{
  "name": "pocket-advocate",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Pocket Advocate — patient-advocacy web app (Cloudflare Worker + Firebase + Stripe)",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "seed-slots": "node scripts/seed-slots.mjs",
    "set-admin": "node scripts/set-admin.mjs"
  },
  "devDependencies": {
    "firebase-admin": "^12.7.0",
    "wrangler": "^3.90.0"
  }
}
